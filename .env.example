# Backend BI API base URL. The app always calls this — there is no mock/offline mode.
VITE_API_BASE_URL=http://localhost:4000/api/v1

# Reporting timezone label shown in the UI.
VITE_REPORTING_TZ=America/New_York
